{{- $replicaCount := int .Values.controller.replicaCount }}
{{- if and .Values.controller.podDisruptionBudget.create (gt $replicaCount 1) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "hcloud-csi.names.fullname" . }}-controller
  namespace: {{ include "hcloud-csi.names.namespace" . | quote }}
  labels: {{- include "hcloud-csi.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: controller
    {{- if .Values.commonLabels }}
    {{- include "hcloud-csi.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "hcloud-csi.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.controller.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.controller.podDisruptionBudget.minAvailable }}
  {{- end  }}
  {{- if .Values.controller.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ .Values.controller.podDisruptionBudget.maxUnavailable }}
  {{- end  }}
  selector:
    matchLabels: {{- include "hcloud-csi.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: controller
{{- end }}
